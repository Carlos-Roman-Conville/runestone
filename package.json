{
  "name": "runestone",
  "version": "0.1.0",
  "private": true,
  "description": "Rune-tablet block puzzle. Placeholder name.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "check:layers": "node scripts/check-engine-imports.mjs",
    "check": "npm run check:layers && npm run typecheck && npm run test",
    "dev": "vite",
    "build": "vite build",
    "golden:update": "node scripts/update-golden.mjs",
    "sim": "tsx tools/sim/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^22.20.4",
    "tsx": "^4.23.15",
    "typescript": "^5.6.0",
    "vite": "^5.4.21",
    "vitest": "^2.1.0"
  },
  "dependencies": {
    "pixi.js": "^8.21.0"
  }
}
